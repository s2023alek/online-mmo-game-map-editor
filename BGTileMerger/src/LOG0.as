package  {
	
	//{ =*^_^*= import
	//} =*^_^*= END OF import
	
	/**
	 * @param	m msg
	 * @param	l level
	 */
	public function LOG0(m:String, i:uint=0):void {
		LOGGER0.log(m, i);
	}
}

//{ =*^_^*= History
/* > (timestamp) [ ("+" (added) ) || ("-" (removed) ) || ("*" (modified) )] (text)
 * > 
 */
//} =*^_^*= END OF History

// template last modified:11.03.2011_[18#51#40]_[5]